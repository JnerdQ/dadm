<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JuegoTriqui">

        <!-- Main Entry Point -->
        <activity
            android:name=".MainMenuActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity for Playing Against AI -->
        <activity
            android:name=".MainActivityAI"
            android:exported="false" />

        <!-- Activity for Online Games -->
        <activity
            android:name=".MainActivityOnline"
            android:exported="false" />

        <!-- Lobby Activity for Online Games -->
        <activity
            android:name=".GameLobbyActivity"
            android:exported="false" />

    </application>
</manifest>
