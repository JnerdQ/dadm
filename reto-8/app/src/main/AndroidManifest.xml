<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Empresa"
        tools:targetApi="31">
        <activity
            android:name=".ui.components.CompanyDialog"
            android:exported="false"
            android:label="@string/title_activity_company_dialog"
            android:theme="@style/Theme.Empresa" />
        <activity
            android:name=".ui.screens.CompanyScreen"
            android:exported="false"
            android:label="@string/title_activity_company_screen"
            android:theme="@style/Theme.Empresa" />
        <activity
            android:name=".ui.screens.EmpresaFormScreen"
            android:exported="false"
            android:label="@string/title_activity_empresa_form_screen"
            android:theme="@style/Theme.Empresa" />
        <activity
            android:name=".ui.screens.EmpresaListScreen"
            android:exported="false"
            android:label="@string/title_activity_empresa_list_screen"
            android:theme="@style/Theme.Empresa" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Empresa">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>